<!DOCTYPE html>
<html>
<head>
  <title><%= yield(:title) || "Learning Management System" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Latest compiled JavaScript -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
<div class="container">
  <%= render 'shared/navbar' %>
  <%= render 'shared/show_flash_message' %>
  <div class="card mt-5">
    <div class="card-header bg-primary">
      <div class="row">
        <div class="col-9">
          <h5 style="color: #ffffff; font-style: italic"><%= @page_title || "Dashboard" %></h5>
        </div>
        <div class="col-2">
          <div class="row">
            <div class="col">
              <h5 class="text-light">Mr. <%= current_user.name %></h5>
            </div>
          </div>
          <div class="row">
            <div class="col">
              <span class="badge badge-secondary text-light">Role: <%= current_user.role %></span>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="card-body">
      <div class="row">
        <div class="col-10">
          <%= yield %>
        </div>
        <div class="col-2">
          <%= render 'shared/navbar_right' %>
        </div>
      </div>
    </div>
  </div>
</div>
</body>
</html>
